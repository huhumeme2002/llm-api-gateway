# handoff_history

`scripts/agent_handoff.py` copies the previous `.agent/HANDOFF.md` here as `HANDOFF-<UTC timestamp>.md` before writing a fresh template. Keep these files free of secrets and conversation dumps.
